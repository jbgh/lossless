# Changelog

## 0.1.0

First release.

- Local tape + derived claims + `ask` (≤5). Default install is this machine only.
- Hooks, MCP, skill, and a user service for Grok, Claude, Codex, Pi, and OpenCode.
- `inspect` / `inspect --prune` for tape vs claims vs last packs.
- Release channel: GitHub Releases. `lossless update` and `scripts/install.sh` pull `lossless-<os>-<arch>` after checking `SHA256SUMS`. `doctor` does not phone home.
