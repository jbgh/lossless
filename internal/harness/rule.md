# lossless

Before implementing, changing behavior, or continuing after compact, call the lossless MCP tool `ask` (may appear as `lossless__ask`) with `workspace_root`, `goal`, and `paths`. Treat `warnings` as blocking unless the user overrides. Do not wait for the user to mention lossless. Skip trivia and chit-chat. The user should not have to type `/lossless`.
